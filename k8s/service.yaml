apiVersion: v1
kind: Service
metadata:
  name: invirtus/konva
  labels:
    app: invirtus/konva
spec:
  selector:
    app: invirtus/konva
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
