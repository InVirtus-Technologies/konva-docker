apiVersion: v1
kind: Service
metadata:
  name: konva-renderer
  labels:
    app: konva-renderer
spec:
  selector:
    app: konva-renderer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
